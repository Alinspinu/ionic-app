run npm install in each file (server, front)
must install ionic-cli globaly:  npm i -g @ionic/cli
must have mongosh instaled localy
you need 2 terminal windows, cd in each file and start the apps: server: node index.js; front: ionic-serve
